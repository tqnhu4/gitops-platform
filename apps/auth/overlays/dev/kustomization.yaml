resources:
  - ../../base

namespace: auth-dev
nameSuffix: -dev

images:
  - name: nhutq/auth-service
    newTag: latest

replicas:
  - name: auth
    count: 1

configMapGenerator:
  - name: auth-config
    literals:
      - ENV=dev